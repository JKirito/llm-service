{
  "name": "llm-service-monorepo",
  "version": "1.0.0",
  "description": "A Bun monorepo for LLM services",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*",
    "apps/*",
    "tools/*"
  ],
  "scripts": {
    "build": "bun run build:packages && bun run build:apps",
    "build:packages": "bun run build --filter=\"@llm-service/*\"",
    "build:apps": "bun run build --filter=\"@llm-apps/*\"",
    "dev": "bun run --filter=\"@llm-apps/api\" dev & bun run --filter=\"@llm-apps/web-app\" dev",
    "dev:api": "bun run --filter=\"@llm-apps/api\" dev",
    "dev:web": "bun run --filter=\"@llm-apps/web-app\" dev",
    "start:api": "bun run --filter=\"@llm-apps/api\" start",
    "start:web": "bun run --filter=\"@llm-apps/web-app\" start",
    "test": "bun run test --filter=\"*\"",
    "lint": "bun run lint --filter=\"*\"",
    "clean": "bun run clean --filter=\"*\" && rm -rf node_modules",
    "typecheck": "bun run typecheck --filter=\"*\"",
    "bootstrap": "bun install"
  },
  "devDependencies": {
    "@types/bun": "^1.1.0",
    "@typescript-eslint/eslint-plugin": "^7.0.0",
    "@typescript-eslint/parser": "^7.0.0",
    "eslint": "^9.0.0",
    "eslint-config-prettier": "^9.0.0",
    "eslint-plugin-prettier": "^5.0.0",
    "prettier": "^3.3.0",
    "typescript": "^5.5.0"
  },
  "engines": {
    "bun": ">=1.1.0"
  },
  "packageManager": "bun@1.1.0"
}
